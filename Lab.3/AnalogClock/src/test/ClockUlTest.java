import org.clock.ui.ClockUI;

public class ClockUlTest {
    public static void main(String[] args) {
        ClockUI clock = new ClockUI();
        clock.setSecond (00);
        clock.setMinute(00 );
        clock.setHour(00);
        clock.setClock(00,00,00);


    }
}
